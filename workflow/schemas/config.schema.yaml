$schema: "http://json-schema.org/draft-06/schema#"

description: snakemake configuration file

type: object

properties:
    samples:
          type: string
    adapters:
          type: string
    threads:
          type: integer

required:
  - samples
  - adapters
